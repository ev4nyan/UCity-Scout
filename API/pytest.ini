[pytest]
testpaths = tests
markers =
    smoke: marks tests that hit live external APIs (deselect with '-m "not smoke"')
    unit: marks pure unit tests with no external dependencies
    integration: marks tests that require a database connection
